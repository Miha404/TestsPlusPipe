pipeline {
    agent any

    stages {
        stage('Run tests') {
            steps {
                sh "./gradlew uitest_chrome"
            }
        }

    post {
        always {
            allure([
                reportBuildPolicy: 'ALWAYS',
                results:[[path:'allure-results']]
            ])
        }
    }
}